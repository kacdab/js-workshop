const assert = require('assert');
const ExerciseName = require('../src/main');

describe('ExerciseName', () => {
  it('is defined', () => {
    assert.equal(typeof ExerciseName, 'function');
  });
});
